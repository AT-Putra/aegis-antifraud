# =====================================================================
# Aegis Anti Fraud — template environment. Salin ke `.env` lalu isi.
# `.env` TIDAK di-commit (lihat .gitignore). Jangan taruh rahasia di sini.
# =====================================================================

APP_ENV=development

# --- PostgreSQL (OLTP) ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=aegis
POSTGRES_PASSWORD=change-me
POSTGRES_DB=aegis

# --- ClickHouse (OLAP) ---
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=aegis
CLICKHOUSE_PASSWORD=change-me
CLICKHOUSE_DB=aegis

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Keamanan (WAJIB diisi nilai acak kuat ≥32 byte di produksi) ---
SESSION_SIGNING_KEY=dev-change-me-to-a-random-32-byte-min-secret
JWT_SECRET=dev-change-me-to-a-random-32-byte-min-secret
SECRET_ENC_KEY=dev-change-me-to-a-random-32-byte-min-secret    # master key AES-256-GCM utk hmac_secret (TQ-08)

# --- Lokal & origin ---
TZ_DEFAULT=Asia/Jakarta
ALLOWED_ORIGINS=http://localhost

# --- Model store (shared, ADR-003) ---
MODEL_DIR=/models

# --- IP intelligence (TQ-07: GeoLite2 + IP2Proxy LITE) ---
# Daftar akun gratis untuk dapat kredensial unduh DB (file TIDAK di-commit).
MAXMIND_ACCOUNT_ID=
MAXMIND_LICENSE_KEY=
IP2LOCATION_TOKEN=
GEOIP_DIR=/data/geoip          # lokasi file DB (.mmdb / .BIN) di container

# --- Caddy / TLS ---
# Dev: http://localhost (tanpa TLS). JANGAN dikosongkan — Caddy butuh alamat valid.
# Produksi: CADDY_DOMAIN=fraud.contoh.com  &  CADDY_EMAIL=ops@contoh.com (TLS otomatis)
CADDY_DOMAIN=http://localhost
CADDY_EMAIL=

# =====================================================================
# Batas memori PARAMETRIK (mem_limit). Default di bawah = PROFIL DEV (≈8 GB host).
# ---------------------------------------------------------------------
# Profil DEV (mesin development spek rendah):
POSTGRES_MEM_LIMIT=1g
CLICKHOUSE_MEM_LIMIT=2g
REDIS_MEM_LIMIT=512m
REDIS_MAXMEMORY=384mb
API_MEM_LIMIT=1500m
CADDY_MEM_LIMIT=256m
#
# Profil PRODUKSI (TRD §7, host ≥12 GB) — pakai nilai ini di .env produksi:
#   POSTGRES_MEM_LIMIT=2g
#   CLICKHOUSE_MEM_LIMIT=4g
#   REDIS_MEM_LIMIT=512m
#   REDIS_MAXMEMORY=448mb
#   API_MEM_LIMIT=2g
#   CADDY_MEM_LIMIT=300m
# (ClickHouse otomatis pakai 70% dari mem_limit-nya — infra/clickhouse/config.d/memory.xml)
# =====================================================================
