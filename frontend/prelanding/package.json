{
  "name": "aegis-prelanding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Aegis pre-landing portabel (T-10) — kumpul sinyal, scoring, redirect/block",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.0.0",
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0",
    "msw": "^2.6.0"
  }
}
